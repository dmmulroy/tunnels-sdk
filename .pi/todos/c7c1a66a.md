{
  "id": "c7c1a66a",
  "title": "Fix code review issues across tunnels SDK",
  "tags": [
    "review",
    "cleanup",
    "bugfix",
    "tunnels"
  ],
  "status": "closed",
  "created_at": "2026-03-28T23:45:35.265Z"
}

Fixed correctness and maintainability issues across `packages/tunnels`:

- fixed DELETE query-param support in `ApiClient`
- fixed `Tunnel.delete()` force + DNS cleanup order
- fixed `expose()` custom binary handling
- replaced ESM-breaking `require()` in config loader with static yaml import
- simplified/normalized cloudflared download logic and version handling
- added `Tunnel.refresh()` for stale snapshot issue
- exported `Tunnel`, `TunnelProcess`, and `LogStream`
- removed misleading quick-expose options from public types/docs
- reduced AI-slop comments/JSDoc/decorative banners across source files
- tightened `TunnelProcess` event typing and parsing
- expanded tests to cover client, tunnel, managers, and yaml helpers
- added `LICENSE` and package README

Validation:
- `npx tsc --noEmit`
- `npx vitest run` (62 tests)
- `npx tsdown`
