{
  "id": "01a75219",
  "title": "Effect refactor: Wire tunnels CLI to real tunnels/effect services",
  "tags": [
    "effect-refactor",
    "integration"
  ],
  "status": "closed",
  "created_at": "2026-03-29T02:52:03.601Z"
}

## Goal

Replace the `tunnels-cli` CLI's placeholder `LiveLayer` (currently all `notImplemented` stubs) with real implementations backed by the `tunnels/effect` services. This connects the CLI to the real Cloudflare API.

## Context

The `tunnels-cli` CLI package (`packages/cli/`) already uses Effect. It defines its own service interfaces in `src/services.ts` (`QuickTunnelService`, `TunnelApiService`, `IngressService`, `RouteService`, `DnsService`, `VNetService`, `ConfigService`, `AuthService`) and has a `live-layer.ts` that provides stubs that all return `notImplemented`.

The goal is to create **adapter layers** that bridge the CLI service interfaces to the `tunnels/effect` services. Some CLI services map 1:1 to SDK services; others (like `ConfigService`, `AuthService`) are CLI-specific and stay as stubs or get their own implementation.

## What to do

### 1. Read and understand the CLI service interfaces

Read `packages/cli/src/services.ts` carefully. Map each CLI service method to the corresponding SDK service method:

| CLI service | SDK service | Notes |
|---|---|---|
| `QuickTunnelService.expose(port)` | `expose(port)` from tunnels/effect | Returns `{ url }` |
| `TunnelApiService.create(name, opts?)` | `TunnelOperations.create(name, opts?)` | Map `TunnelInfo` → `TunnelInfo` (CLI type) |
| `TunnelApiService.list(opts?)` | `TunnelOperations.list(opts?)` | |
| `TunnelApiService.get(ref)` | `TunnelOperations.get(ref)` | |
| `TunnelApiService.delete(ref, opts?)` | `TunnelOperations.delete(ref, opts?)` | |
| `TunnelApiService.run(ref, opts?)` | `TunnelOperations.getToken` + `TunnelProcessService.run` | Needs composition |
| `TunnelApiService.stop(ref)` | Close the running tunnel's scope | Needs state management |
| `TunnelApiService.getLogs(ref)` | Read from RunningTunnel.logs stream | |
| `TunnelApiService.getToken(ref)` | `TunnelOperations.getToken(ref)` | |
| `IngressService.*` | `IngressManager.*` | Need active tunnel ID context |
| `RouteService.*` | `RouteManager.*` | Need active tunnel ID context |
| `DnsService.*` | `DnsManager.*` | Need tunnel ID context |
| `VNetService.*` | `VNetManager.*` | Direct mapping |
| `ConfigService.*` | CLI-specific | Keep as-is or implement separately |
| `AuthService.*` | CLI-specific | Keep as-is or implement separately |

### 2. Update `packages/cli/src/live-layer.ts`

Replace the stubs with real adapter implementations. Import from `tunnels/effect`:

```ts
import { Effect, Layer } from "effect"
import {
  TunnelOperations, IngressManager, DnsManager, RouteManager, VNetManager,
  TunnelProcessService, CloudflaredBinary, LiveLayerFromEnv, expose,
} from "tunnels/effect"
```

For services that map cleanly (VNetService, etc.), create adapter layers:
```ts
const VNetServiceLive = Layer.effect(
  VNetService,
  Effect.gen(function*() {
    const vnets = yield* VNetManager
    return {
      create: Effect.fn(function*(name, opts?) {
        yield* vnets.create(name, { default: opts?.isDefault, comment: undefined })
      }),
      list: Effect.fn(function*() {
        const results = yield* vnets.list()
        return results.map(v => ({ name: v.name, isDefault: v.isDefault }))
      }),
      delete: Effect.fn(function*(name) {
        yield* vnets.delete(name)
      }),
    }
  })
)
```

For `TunnelApiService.run` and `TunnelApiService.stop`, you'll need to manage a running tunnel reference (possibly via `Ref` or `FiberHandle`).

### 3. Update `packages/cli/package.json`

Add `tunnels` as a dependency (workspace link):
```json
{
  "dependencies": {
    "tunnels": "workspace:*"
  }
}
```

### 4. Map errors

The tunnels-cli uses `CliError` (from `packages/cli/src/errors.ts`). SDK services throw `TunnelApiError`, `TunnelAuthError`, etc. The adapter layers need to catch SDK errors and wrap them in `CliError`:

```ts
Effect.catchTags({
  TunnelApiError: (e) => CliError.ApiError({ message: `API error ${e.status}` }),
  TunnelAuthError: (e) => CliError.AuthError({ message: e.message }),
  TunnelNotFoundError: (e) => CliError.NotFoundError({ message: `Tunnel not found: ${e.tunnelRef}` }),
})
```

### 5. Keep ConfigService and AuthService as stubs (or implement separately)

These are CLI-specific services that don't have SDK counterparts. They can stay as `notImplemented` stubs for now, or be implemented as a follow-up.

## Files to read first

- `packages/cli/src/services.ts` — CLI service interfaces (source of truth for what to implement)
- `packages/cli/src/errors.ts` — `CliError` type
- `packages/cli/src/live-layer.ts` — current stubs to replace
- `packages/cli/src/test-layer.ts` — test layer pattern
- `packages/tunnels/src/effect/index.ts` — SDK exports
- `packages/cli/package.json`

## Acceptance criteria

- [ ] `QuickTunnelService` wired to `expose`
- [ ] `TunnelApiService` CRUD methods wired to `TunnelOperations`
- [ ] `IngressService`, `RouteService`, `DnsService`, `VNetService` wired to SDK managers
- [ ] SDK errors mapped to `CliError` variants
- [ ] `ConfigService` and `AuthService` remain stubs (with TODO comments)
- [ ] `tunnels` added as workspace dependency
- [ ] `pnpm typecheck` passes for tunnels-cli package
- [ ] Existing tunnels-cli tests still pass

## Dependencies

- Requires: TODO-b0c6986e (layers + effect/index.ts — the full SDK)
- Requires: TODO-ddfa1ec8 (cleanup — old code removed, clean exports)
