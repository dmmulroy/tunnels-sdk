{
  "name": "tunnels-monorepo",
  "private": true,
  "scripts": {
    "start": "node scripts/serve-learning-site.mjs",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  }
}
